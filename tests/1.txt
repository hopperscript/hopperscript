define var "test \"object\""
define object "idk" = objects.Square
define object "more" = objects.Square

for "idk" {
  define var "hello"

  when game_starts() {
    move_forward("20")
    move_forward(v"test \"object\"")
  }

  when is_tapped(o"more") {
    destroy()
  }
}